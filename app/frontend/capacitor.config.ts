import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'frontend',
  webDir: '../../dist/app/frontend',
  bundledWebRuntime: false,
};

export default config;
